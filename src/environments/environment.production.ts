export const environment = {
  production: true,
  useMockApi: false,
  apiBaseUrl: '/api/v1',
  mockDataPath: 'assets/mock-data',
  mockDelayMs: 0,
  chatPollingIntervalMs: 5000,
  dashboardRefreshIntervalMs: 30000,
  enableMockControls: false,
  currency: 'INR',
  timezone: 'Asia/Kolkata'
};
